az login

# Before we can use commands within az containerapp we have to install the extension by running the following command
az extension add --source https://workerappscliextension.blob.core.windows.net/azure-cli-extension/containerapp-0.2.4-py2.py3-none-any.whl

# Truncate SensorData table